import {createWindow} from "./createWindow";

export const aboutWindow = createWindow({
    title: 'About Qommand',
    route: 'about',
    width: 320,
    height: 180,
    minWidth: 320,
    minHeight: 180,
});
